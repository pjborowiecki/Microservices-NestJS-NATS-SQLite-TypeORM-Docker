# Microservices with NestJS, NATS, SQLite, TypeORM and Docker

#### Docker commands:

- `docker-compose up --build`

- `mysql -u $username -p`
- `show databases;`
- `use $database_name`
- `show tables`
